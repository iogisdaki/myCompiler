START	LDA x
	STA T0
	LDA =5=
	STA T1
	LDA T0
	CMPA T1
	JE TRUE
	LDA  =0=
	JMP DONE
TRUE LDA  =1=
DONE NOP
	CMPA 0
	JE ELSE
	LDA =4=
	STA a
	JMP ENDIF
ELSE	LDA i
	STA T2
	LDA =32=
	SUB T2
	STA b
ENDIF NOP
	END  START